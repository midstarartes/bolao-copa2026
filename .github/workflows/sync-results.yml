name: Sincronizar resultados da Copa (desativado)

concurrency:
  group: sync-resultados-copa
  cancel-in-progress: false

on:
  workflow_dispatch:

jobs:
  sync-results-disabled:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Sincronizacao automatica desativada
        run: |
          echo "A sincronizacao automatica por API esta desativada."
          echo "Os resultados oficiais devem ser lancados manualmente pelo painel ADMIN."
